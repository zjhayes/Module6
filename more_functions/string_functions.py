
def multiply_strings(message, n):
    """Takes a message and echoes it back a given number of times.
        :param message, a string to be echoed.
        :param n, the number of times message will be echoed.
        :returns the message multiplied as a string
        """
    return message * n
